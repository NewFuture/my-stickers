import { makeStyles } from "@fluentui/react-components";

export const useUploadButtonStyles = makeStyles({
    root: {
        position: "relative",
        textAlign: "center",
    },
    icon: {
        width: "100%",
        height: "100%",
    },
});
